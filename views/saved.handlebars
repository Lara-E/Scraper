<ul class="nav">
    <li class="nav-item">
        <a class="nav-link" href="/">Home</a>
    </li>
    <li class="nav-item">
        <a class="nav-link active" href="/saved">Saved</a>
    </li>
    <h1 class="nfl ml-4">NFL Scraper</h1>
</ul>
<div class="container">
    <div class="row">
        <div class="col-lg-12">
        </div>
    </div>
    <div class="row">
        {{#each article}}
        {{#if saved}}
        <div class="col-lg-9">
            <h3><a href="http://espn.com{{link}}">{{title}}</a></h3>
            <p>{{summary}}</p>
            <button type="button" class="btn btn-primary add-note" data-id="{{_id}}">Add Note</button>
            <button type="button" class="btn btn-primary delete-article" data-id="{{_id}}">Delete Article</button>
            <hr>
        </div>
        <div class="col-lg-3">
            <div id="notes">
            </div>
        </div>
        {{/if}}
        {{/each}}
    </div>
</div>